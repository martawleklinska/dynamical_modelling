# Dynamical systems modelling

## Project initialization
Project involves Duffing oscillator solver.
One can run the project using CMake, so
```
cd projekt_Duffing/
mkdir build && cd build/
cmake .. && make
./main
```

## TODO
1. Add quantum simulations:
    - solve TDSE;
    - find how WDF applies here.